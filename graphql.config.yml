schema: "./vite_ui/schema.graphql"

documents: "./vite_ui/src/**/*.{graphql}"

extensions:
  endpoints:
    Default GraphQL Endpoint:
      url: http://localhost:9016/graphql
      headers:
        user-agent: JS GraphQL
      introspect: false
