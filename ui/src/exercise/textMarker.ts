import {NounMatchingResultFragment} from '../graphql';
import {SideSelector} from './NewCorrectSolutionContainer';

export function markText(text: string, side: SideSelector, {matches}: NounMatchingResultFragment): string {

  // console.info(JSON.stringify(matches));


  return text;
}
